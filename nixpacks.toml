[variables]
NIXPACKS_PKGS = "php82 php82Extensions.mysqli php82Extensions.pdo php82Extensions.pdo_mysql"

[phases.setup]
nixPkgs = ["php82", "php82Extensions.mysqli", "php82Extensions.pdo", "php82Extensions.pdo_mysql"]

[phases.build]
cmds = ["echo 'PHP project with MySQL extensions'"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t ."
